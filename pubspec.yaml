name: geometry_3d_app
description: 高中立体几何交互式学习App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_3d_view: ^0.2.0
  vector_math: ^2.1.4
  provider: ^6.0.5
  flutter_animate: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/